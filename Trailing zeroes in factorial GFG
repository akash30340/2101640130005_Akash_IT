int trailingZeroes(int n)
    {
        // Write Your Code here
        long long count=0;
        int i=1;
        while(1)
        {
            count+=n/pow(5,i);
            i++;
            if(n/pow(5,i)==0)
            break;
        }
        return count;
    }
